version: "3.9"

services:
  app:
    build: .
    restart: always
    depends_on:
      - redis
    entrypoint:
      - ./bin/entrypoint.sh
  redis:
    image: redis:alpine
    volumes: 
      - ./redis.conf:/usr/local/etc/redis/redis.conf
    ports:
      - "6379:6379"